<UserControl x:Class="CineTrack.MVVM.View.WatchedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <StackPanel Margin="10">
            <TextBlock FontWeight="Bold" FontSize="16" FontFamily="/Fonts/#Poppins" Foreground="White">
                <Run Text="{Binding MovieCount, StringFormat=Nombre de films : {0}}"/>
                <Run Text=" | "/>
                <Run Text="{Binding TvShowCount, StringFormat=Nombre de séries : {0}}"/>
                <Run Text=" | "/>
                <Run Text="{Binding TotalCount, StringFormat=Total : {0}}"/>
            </TextBlock>
            <ListBox ItemsSource="{Binding MediaList}" Margin="0, 10, 0, 0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <StackPanel Margin="10,0,0,0">
                                <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding ReleaseYear}"/>
                                <TextBlock Text="{Binding MediaType}"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>