<UserControl x:Class="CineTrack.MVVM.View.WatchedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#2A273F" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Statistiques -->
        <TextBlock Grid.Row="0"
                   FontWeight="Bold"
                   FontSize="18"
                   FontFamily="/Fonts/#Poppins"
                   Foreground="White"
                   Margin="0,0,0,10">
            <Run Text="{Binding MovieCount, StringFormat=Nombre de films : {0}}"/>
            <Run Text="  |  "/>
            <Run Text="{Binding TvShowCount, StringFormat=Nombre de séries : {0}}"/>
            <Run Text="  |  "/>
            <Run Text="{Binding TotalCount, StringFormat=Total : {0}}"/>
        </TextBlock>

        <!-- Liste scrollable -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ScrollViewer.Resources>
                <Style TargetType="ScrollBar">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Width" Value="8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid Background="{TemplateBinding Background}">
                                    <Track Name="PART_Track" IsDirectionReversed="true">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Command="ScrollBar.LineUpCommand"
                                                          Background="Transparent"
                                                          IsEnabled="False"
                                                          Height="0"/>
                                        </Track.DecreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb>
                                                <Thumb.Template>
                                                    <ControlTemplate TargetType="Thumb">
                                                        <Border Background="#B0B0B0" CornerRadius="4"/>
                                                    </ControlTemplate>
                                                </Thumb.Template>
                                            </Thumb>
                                        </Track.Thumb>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Command="ScrollBar.LineDownCommand"
                                                          Background="Transparent"
                                                          IsEnabled="False"
                                                          Height="0"/>
                                        </Track.IncreaseRepeatButton>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ScrollViewer.Resources>

            <!-- Liste des éléments -->
            <ItemsControl ItemsSource="{Binding MediaList}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="10"
                                Margin="0,5"
                                Background="#FFFFFF"
                                Padding="10">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                            </Border.Effect>

                            <DockPanel>
                                <Image Source="{Binding PosterPath}" Width="80" Height="120" Stretch="UniformToFill" Margin="0,0,10,0"/>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="16" Foreground="#1E1B2E" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding ReleaseYear}" FontStyle="Italic" FontSize="14" Foreground="Gray" Margin="0,5,0,0"/>
                                    <TextBlock Text="{Binding MediaType}" FontSize="14" Foreground="#555"/>
                                </StackPanel>
                            </DockPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
