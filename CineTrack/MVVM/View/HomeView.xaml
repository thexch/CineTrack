<UserControl x:Class="CineTrack.MVVM.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CineTrack.MVVM.View"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#F5F5F5">
    <!-- Fond clair pour un look moderne -->

    <Grid>
        <StackPanel Margin="20" HorizontalAlignment="Center">
            <TextBlock FontWeight="Bold" FontSize="24" Margin="0,0,0,20" Foreground="#333333">
                Bonjour, voici vos statistiques globales :
            </TextBlock>

            <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock FontWeight="Bold" FontSize="18" Margin="0,0,0,10" Foreground="#555555">
                        À voir :
                    </TextBlock>
                    <TextBlock FontSize="16" Foreground="#666666">
                        <Run Text="{Binding TotalMoviesToWatch, StringFormat=Nombre de films : {0}}"/>
                        <Run Text=" | "/>
                        <Run Text="{Binding TotalTvShowsToWatch, StringFormat=Nombre de séries : {0}}"/>
                        <Run Text=" | "/>
                        <Run Text="{Binding TotalToWatch, StringFormat=Total : {0}}"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock FontWeight="Bold" FontSize="18" Margin="0,0,0,10" Foreground="#555555">
                        En cours :
                    </TextBlock>
                    <TextBlock FontSize="16" Foreground="#666666">
                        <Run Text="{Binding TotalMoviesInProgress, StringFormat=Nombre de films : {0}}"/>
                        <Run Text=" | "/>
                        <Run Text="{Binding TotalTvShowsInProgress, StringFormat=Nombre de séries : {0}}"/>
                        <Run Text=" | "/>
                        <Run Text="{Binding TotalInProgress, StringFormat=Total : {0}}"/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="5" Padding="15">
                <StackPanel>
                    <TextBlock FontWeight="Bold" FontSize="18" Margin="0,0,0,10" Foreground="#555555">
                        Vus :
                    </TextBlock>
                    <TextBlock FontSize="16" Foreground="#666666">
                        <Run Text="{Binding TotalMoviesWatched, StringFormat=Nombre de films : {0}}"/>
                        <Run Text=" | "/>
                        <Run Text="{Binding TotalTvShowsWatched, StringFormat=Nombre de séries : {0}}"/>
                        <Run Text=" | "/>
                        <Run Text="{Binding TotalWatched, StringFormat=Total : {0}}"/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
